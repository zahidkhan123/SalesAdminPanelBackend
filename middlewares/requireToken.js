// const { useErrorResponse } = require('../utils/apiResponse');

// const requireToken = (req, res, next) => {
//   if (!req.user) {
//     return useErrorResponse(res, 'you are not logged in', 401);
//   }
//   next();
// };

// module.exports = {
//   requireToken,
// };
